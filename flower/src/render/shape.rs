use crate::Px;

///
/// 图形坐标，绘制时使用
///
pub struct ShapeCoord {}

impl ShapeCoord {
    pub fn from_rect() -> Self {
        Self{}
    }

    pub fn from_origin_and_radius(origin: (Px, Px), radius: Px) -> Self {
        Self{}
    }
}